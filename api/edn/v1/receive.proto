syntax = "proto3";

package edn.v1;

import "google/api/annotations.proto";

option go_package = "SK-builder/api/edn/v1;v1";

service Receiver {
  rpc Recrive (ReceiveRequest) returns (ReceiveReply) {
    option (google.api.http) = {
      get: "/channel/{channel}"
    };
  }
}

message ReceiveRequest {
  string channel = 1;
}

message ReceiveReply {
  string message = 1;
}
